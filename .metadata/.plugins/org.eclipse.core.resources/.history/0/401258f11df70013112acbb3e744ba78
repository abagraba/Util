package com.mimvista.cloud.organizer.verifier.proui;

import java.awt.Component;

import javax.swing.Box;
import javax.swing.BoxLayout;
import javax.swing.JComponent;
import javax.swing.JPanel;
import javax.swing.border.Border;
import javax.swing.border.EmptyBorder;

public class VerifierUtil {

	private static int xmargin = 10;
	private static int ymargin = 10;
	public static Border buttonBorder = new EmptyBorder(xmargin, ymargin, xmargin, ymargin);

	public static JPanel horizontalBox() {
		JPanel panel = new JPanel();
		panel.setLayout(new BoxLayout(panel, BoxLayout.X_AXIS));
		return panel;
	}

	public static JPanel verticalBox() {
		JPanel panel = new JPanel();
		panel.setLayout(new BoxLayout(panel, BoxLayout.Y_AXIS));
		return panel;
	}

	public static void insert(JComponent component, JPanel container) {
		container.add(component);
		component.setAlignmentX(Component.CENTER_ALIGNMENT);
		component.setAlignmentY(Component.CENTER_ALIGNMENT);
	}

	public static void addStrut(JPanel panel, int size) {
		if (panel.getLayout() instanceof BoxLayout) {
			int axis = ((BoxLayout) panel.getLayout()).getAxis();
			switch (axis) {
				case BoxLayout.X_AXIS:
					panel.add(Box.createHorizontalStrut(size));
					break;
				case BoxLayout.Y_AXIS:
					panel.add(Box.createVerticalStrut(size));
					break;
			}
		}
	}

	public static void addGlue(JPanel panel) {
		panel.add(Box.createGlue());
	}
}
